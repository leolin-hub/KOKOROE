# 複製成 .env 後生效（.env 已被 .gitignore 忽略，不會進版控）
#
# API 的 base path。預設走 Vite dev proxy（見 vite.config.ts），
# 所以是相對路徑；若要直接打某台後端，改成完整 URL，例如：
#   VITE_API_BASE_URL=https://api.kokoroe.example.com/api/v1
#
# 注意：只有 VITE_ 前綴的變數會被打包進前端 bundle。
# 這也意味著「任何寫在這裡的東西都是公開的」—— 永遠不要放密鑰。
VITE_API_BASE_URL=/api/v1
